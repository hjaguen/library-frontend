# Set the API URL environment variable
$env:VITE_API_URL = "http://localhost:3000/api"

# Build the frontend
npm run build

# You can use a static file server to serve the dist directory
# For development, you can use npm run dev instead
